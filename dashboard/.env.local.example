# Where the engine is, in development. The dev server proxies /api/ to its
# /internal/ and adds the token, so the token never reaches the browser.
ENGINE_URL=http://localhost:3000
INTERNAL_TOKEN=dev-internal-token
